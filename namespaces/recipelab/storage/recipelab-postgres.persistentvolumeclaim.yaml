apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recipelab-postgres-pvc
  namespace: recipelab
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
